#include "zad1.hpp"

#include <algorithm>
#include <deque>
#include <vector>

// tutaj algorytm liczKolejneWarzywa
template <typename Iterator>
bool liczKolejnewarzywa(Iterator begin, Iterator end, unsigned n, const std::string s) {
   //return std::search_n(begin, end, )
}
